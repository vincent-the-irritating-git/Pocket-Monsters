
Pokemon user
Pokemon enemy
Pokemon[]turn_order
Move move_selected



Turn(int pokemon_current_turn){

check_first_to_move(){
//see who is (user or enemy) faster and put them at position 0 in the array; 
//other in 1

}

turn_order = check_first_to_move();

Move select_move(){
	pokemon_array[0].list_moves()
	return display_moves(Pokemon pokemon)
}

move_selected=select_move();

check_target(Move move){
//if true, move hits enemy, else us
move_selected.is_target_enemy
if (true)
use_move(user, enemy, move_selected())
else
use_move(user, user, move_selected())
}

use_move(Pokemon attacking, Pokemon defending, Move move){
//do damage
//inflict status
//change stages

int damage=(move.power*attacking.attack/defending.defence)/50+2*critical()*random()*STAB*type*burn

}

}