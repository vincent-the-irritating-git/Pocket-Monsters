Battle(Pokemon user, Pokemon enemy){

Pokemon[]turn_order
Move move_selected

turn_order=Pokemon[] check_first_to_move(user.speed, enemy.speed){
//see who is faster and put them at position 0 in the array; 
//opponent in 1
}

move_selected=Move select_move(){
	display_moves(Pokemon pokemon){
	//lists the users moves
	pokemon_array[0].list_moves()
	}
}

check_target(Move move){
//if true, move hits enemy, else us
move_selected.is_target_enemy
if (true)
use_move(user, enemy, move_selected())
else
use_move(user, user, move_selected())
}

use_move(Pokemon attacking, Pokemon defending, Move move){
//do damage
//inflict status
//change stages
int damage=(move.power*attacking.attack/defending.defence)/50+2*critical()*random()*STAB*type*burn

}

}